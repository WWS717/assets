#!/bin/python3

import os
	for root, subs, files in os.path(start):
	for file in files : 
	print( os.path.join(root, file) )
   	# Modify directories in place to remove directories starting with '.'
	for dir in subs[:}:
		if dir.startswith('.'):
		subs.remove(dir)
